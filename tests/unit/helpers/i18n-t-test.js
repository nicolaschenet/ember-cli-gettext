import { i18nT } from '../../../helpers/i18n-t';
import { module, test } from 'qunit';

module('Unit | Helper | i18n t');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = i18nT(42);
  assert.ok(result);
});
